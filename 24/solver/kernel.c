static inline __attribute__((always_inline)) int64_t kernel(
	 const uint8_t digit1,
	 const uint8_t digit2,
	 const uint8_t digit3,
	 const uint8_t digit4,
	 const uint8_t digit5,
	 const uint8_t digit6,
	 const uint8_t digit7,
	 const uint8_t digit8,
	 const uint8_t digit9,
	 const uint8_t digit10,
	 const uint8_t digit11,
	 const uint8_t digit12,
	 const uint8_t digit13,
	 const uint8_t digit14
) {
	int64_t x = 0;
	int64_t y = 0;
	int64_t z = 0;
	int64_t w = 0;

	w = digit1;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 1ll;
	x += 12ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 6ll;
	y *= x;
	z += y;
	w = digit2;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 1ll;
	x += 11ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 12ll;
	y *= x;
	z += y;
	w = digit3;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 1ll;
	x += 10ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 5ll;
	y *= x;
	z += y;
	w = digit4;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 1ll;
	x += 10ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 10ll;
	y *= x;
	z += y;
	w = digit5;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 26ll;
	x += -16ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 7ll;
	y *= x;
	z += y;
	w = digit6;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 1ll;
	x += 14ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 0ll;
	y *= x;
	z += y;
	w = digit7;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 1ll;
	x += 12ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 4ll;
	y *= x;
	z += y;
	w = digit8;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 26ll;
	x += -4ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 12ll;
	y *= x;
	z += y;
	w = digit9;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 1ll;
	x += 15ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 14ll;
	y *= x;
	z += y;
	w = digit10;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 26ll;
	x += -7ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 13ll;
	y *= x;
	z += y;
	w = digit11;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 26ll;
	x += -8ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 10ll;
	y *= x;
	z += y;
	w = digit12;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 26ll;
	x += -4ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 11ll;
	y *= x;
	z += y;
	w = digit13;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 26ll;
	x += -15ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 9ll;
	y *= x;
	z += y;
	w = digit14;
	x *= 0ll;
	x += z;
	x = (x % 26ll + 26ll) % 26ll;
	z /= 26ll;
	x += -8ll;
	x = x == w;
	x = x == 0ll;
	y *= 0ll;
	y += 25ll;
	y *= x;
	y += 1ll;
	z *= y;
	y *= 0ll;
	y += w;
	y += 9ll;
	y *= x;
	z += y;
	return z;
}